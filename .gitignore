# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Local dev notes (don't commit personal notes)
NOTES.md
TODO.md
scratch/

# Node tooling (if we ever migrate to SSG)
node_modules/
.env
.env.local
.cache/

# JSX sources â€” compiled via build_analytics.sh into dist/, not deployed directly
src-jsx/
*.jsx
# Claude Code local tooling
.claude/

# --- 2026-07-17 additions ---
# Scratch / working files (never intended for the repo)
cli.txt
first.txt
ui-ux.txt
gitignore-additions.txt

# Python cache
__pycache__/
*.pyc

# npm — build_analytics.sh uses 
px --yes esbuild, no package.json pinning,
# so these are local scaffolding, not build reproducibility.
package.json
package-lock.json

# Review candidates (regenerable)
member_pubs_candidates.js
tools/chapters_raw.json

# --- 2026-07-17 additions ---
# Scratch / working files (never intended for the repo)
cli.txt
first.txt
ui-ux.txt
gitignore-additions.txt

# Python cache
__pycache__/
*.pyc

# npm — build_analytics.sh uses 
px --yes esbuild, no package.json pinning,
# so these are local scaffolding, not build reproducibility.
package.json
package-lock.json

# Review candidates (regenerable)
member_pubs_candidates.js
tools/chapters_raw.json

# --- 2026-07-17 additions ---
# Scratch / working files (never intended for the repo)
cli.txt
first.txt
ui-ux.txt
gitignore-additions.txt

# Python cache
__pycache__/
*.pyc

# npm — build_analytics.sh uses 
px --yes esbuild, no package.json pinning,
# so these are local scaffolding, not build reproducibility.
package.json
package-lock.json

# Review candidates (regenerable)
member_pubs_candidates.js
tools/chapters_raw.json
